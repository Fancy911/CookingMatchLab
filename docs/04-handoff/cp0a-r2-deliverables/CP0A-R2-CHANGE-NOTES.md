# CP0-A-R2 修改说明

## 订单栏

- 将鸡蛋需求组整体左移，并把两组需求统一为相同的 42px 图标、2px 图文间距、40px 文本框及 `y=132` 基线。
- 鸡蛋需求组内容框结束于 `x=362`；订单托盘可见右边界约为 `x=374`，保留至少 12px 视觉内边距。
- 数值未修改：番茄 `4～6`、鸡蛋 `3～5`。

## 投料槽

- 以三个圆槽的可见中心 `x=68/143/219` 建立统一槽位坐标。
- 番茄、鸡蛋图标分别以第一、第二槽圆心定位；`5份`、`4份` 使用相同宽度和下方基线，均位于各自槽内。
- 第三槽横线以第三槽圆心定位。
- 投料托盘上移至底部安全区内；托盘与开火按钮按可见图形垂直中心对齐。

## 锅层级

- 从现有 `research_pot.png` 裁出独立模块 `research_pot_front.png`，没有生成或重绘新风格素材。
- 节点顺序调整为：`ResearchPotBack → PotIngredients → ResearchPotFrontRim → PotReadyIndicators`。
- 前沿层从原锅图纵向第 134px 开始，运行时遮住番茄和鸡蛋下部约 25%～30%；食材不再覆盖锅柄、锅外壁、三个投料指示孔或处理星。
- 两颗处理星保持 28px，同一基线，并分别与两组食材中心对齐。

## 揭晓覆盖层

- `RevealOverlay` 配置为完整 390×844 的 `UITransform`，并添加 `BlockInputEvents`，拦截覆盖层范围内的全部触摸。
- 遮罩不透明度提升，同时在揭晓开启时关闭底层 `ResearchPot` 和 `ActionArea`；投料托盘、开火按钮及“可开火”文字不再透出，也无法接收触摸。
- 揭晓关闭后恢复底层锅、操作区及验收输入。
- 两个揭晓按钮继续使用同一 `y` 和高度，保持统一底边。

## 料理一致性与临时项

- 已复核订单缩略图和揭晓成品图：只包含番茄和鸡蛋，无葱花或其他未投入食材；本轮未修改料理素材。
- 临时项仍为系统 `PingFang SC` 字体，以及仅用于 CP0-A 验收的隐藏键 `1/2/3` / 查询参数切换。
- 未增加玩法、料理、食材、图鉴、音频、粒子或任何 CP0-B/CP0-C/CP0-D 功能。
