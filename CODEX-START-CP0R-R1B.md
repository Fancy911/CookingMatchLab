# Cooking Match Lab - CP0-R1-B Sole Execution Directive

版本：v0.2  
日期：2026-08-02  
性质：Codex 唯一执行指令  
当前状态：等待用户明确授权后方可执行  
授权范围：仅 CP0-R1-B 可玩循环接入  
禁止范围：CP0-R2、CP0-R3、旧 CP0-D、真实广告、真实支付、正式料理地图与正式音频  
功能基线提交：`184161e3a82e33b929cfa3a709c65409a2ce44ba`  
配置基线：Schema `2` / Hash `a35691f9`  
Cocos Creator：`3.8.8`  
视觉基线：CP0-R1-A Visual Fix 已验收通过

> 本文件是 CP0-R1-B 的唯一执行口径。若旧版 `CP0-R1-Development-Handoff-v0.1.md` 第4章与本文件冲突，以本文件为准。
>
> 未收到以下明确授权文本前，不得修改工程：
>
> `确认CP0-R1-B任务书v0.2，授权Codex严格执行CP0-R1-B；完成全部交付并推送后必须停止，不得开始CP0-R2。`

## 1. 唯一阶段目标

将已经验收通过的 CP0-R1-A 静态视觉壳接入 CP0-R0 唯一 canonical Domain，形成第一版真实可触控、可计时、可连续制作多道料理的 390×844 Cocos 核心试玩。

本阶段必须真实完成：

```text
加载开发研究菜单
→ 90秒有效操作时间开始
→ 玩家真实触摸连线
→ 合法连线只形成1份投料
→ 飞锅、掉落和确定性补盘
→ 连线分、Combo与长连文字反馈
→ 4份后允许主动开火
→ 6份满锅自动开火
→ 料理判定与揭晓
→ 制作次数更新
→ 清锅但保留棋盘、补盘队列、总分与剩余时间
→ 研究线索更新
→ 继续制作下一锅
→ 时间结束自动处理当前锅
→ 本局总结
```

R1-B 必须证明玩家在一局中既能制作不同料理，也能重复制作同一道料理。不得把番茄炒蛋写死为唯一目标或唯一结果。

## 2. 开工保护

开工前必须逐项检查：

1. 当前分支为 `main`，已与 `origin/main` 同步；
2. 功能基线可追溯到 `184161e3a82e33b929cfa3a709c65409a2ce44ba`；
3. 如果本指令先被加入仓库，允许在功能基线之后存在一个仅含本文件的文档提交，但必须记录该提交完整 SHA；
4. 工作区干净，已有 stash 不得读取、应用、修改或删除；
5. Cocos Creator 精确版本为 `3.8.8`；
6. canonical 配置仍为 Schema `2` / Hash `a35691f9`；
7. CP0-R1-A Visual Fix 三张 390×844 截图、录屏和素材真实存在；
8. canonical Domain 仍只存在一份，位置为 `assets/game/scripts/domain/cp0b`；
9. 不存在第二份 `TimedResearchSession`、`RecipeResolver`、`PotModel`、`CookingHistoryModel` 或 `ConfigRegistry`；
10. `assets/game/scripts/application/cp0c/EffectPlan.ts` 当前仍含旧字段 `remainingSteps`，该字段必须在 R1-B 中迁移删除，不得通过新增第二个 EffectPlan 规避；
11. 当前静态壳位于 `CP0ABattleShell.ts`，必须在保留已验收视觉的前提下接入真实 ViewModel，不得推翻重画界面；
12. 如果参考素材、固定场景、字体、配方配置或必要接口缺失，停止报告，不得猜测补齐。

建立并记录 `cp0-r1b-baseline` 标签或等价基线提交。不得改写已有 Git 历史。

## 3. 已冻结的核心规则

R1-B 不得重新讨论或擅自修改以下规则：

| 项目 | 固定口径 |
| --- | --- |
| 棋盘 | 7×7 |
| 连线方向 | 默认八方向，读取配置 |
| 最低合法连线 | 3格 |
| 单次合法连线投料 | 无论3、5、7、9连，均只加入1份对应食材 |
| 单锅容量 | 最少4份可开火，最多6份 |
| 满锅 | 第6份动画结束后约0.6秒检查反馈，然后自动开火 |
| 单局时间 | 90秒有效操作时间 |
| 计时暂停 | 飞锅、掉落、补盘、洗牌、烹饪、揭晓、暂停、后台期间 |
| 超时宽限 | 正在连线时最多1秒 |
| 超时正式料理 | 当前锅4～6份时自动开火 |
| 超时半成品 | 当前锅1～3份，只展示半成品，不进正式料理计数 |
| 空锅超时 | 直接总结 |
| 长连反馈 | 5～6 GOOD；7～8 GREAT；9+ UNBELIEVABLE |
| 灵感 | 7～8连补盘生成；采集仍只投1份并加入INSPIRATION标签 |
| 多锅 | 揭晓后只清锅，保留棋盘、队列、分数、时间与局内记录 |
| 料理判定 | 完全确定，不读取线索目标，不使用随机稀有暴击 |
| 星级 | 不读取当前研究线索是否命中 |
| 同菜重复 | 正常计分并增加本局制作次数；首次发现奖励只发一次 |

如真实接入时发现 Domain 行为与已通过的 R001～R024、A001～A008 或 RS01～RS05 冲突，必须停止并报告，不得在表现层补一套规则，也不得擅自修改 Domain 让画面“看起来正确”。

## 4. R1-B 默认玩家体验

### 4.1 默认入口：不同料理连续制作

默认玩家入口使用 canonical 固定场景 `RS02_MULTI_RECIPE`，或使用与其完全一致的确定性棋盘、补盘队列和操作结果。

一局必须能够按真实触控顺序完成：

1. 第一锅 `香葱土豆饼`；
2. 完整揭晓、记录本局制作1道；
3. 返回同一棋盘与剩余时间；
4. 研究线索自动更新；
5. 第二锅 `田园菌菇汤`；
6. 完整揭晓、记录本局制作第2道；
7. 棋盘、补盘队列、总分和剩余时间均保持连续，不重新开局。

该入口用于证明“玩家一局不是反复完成唯一菜品”。研究线索只提供提示和加分，不限制玩家自由制作其他料理。

### 4.2 重复料理专项入口

隐藏验收入口使用 `RS01_TUTORIAL_REPEAT`：

1. 第一锅 `番茄炒蛋`，完整揭晓，累计制作×1；
2. 返回同局继续；
3. 第二锅仍为 `番茄炒蛋`；
4. 使用约1秒快速揭晓；
5. 显示本次得分、星级和 `本局 ×2`；
6. 首次发现奖励不得重复发放。

### 4.3 超时专项入口

隐藏验收入口使用 `RS05_TIMER_END` 两个 canonical 子用例：

- `FIVE_UNITS_AUTO_FIRE`：5份锅在时间结束后自动开火，结果与主动开火一致；
- `THREE_UNITS_PARTIAL`：3份锅只生成研究半成品，不写入正式料理次数。

### 4.4 长连专项入口

通过 canonical 固定棋盘或独立验收 fixture，允许真实触控稳定完成：

- 一次5连，触发GOOD；
- 一次7连，触发GREAT并验证灵感食材确定性生成；
- 一次9连，触发UNBELIEVABLE与MASTER标签；
- 后续采集灵感食材仍只形成1份投料。

隐藏入口只能通过查询参数、键盘或测试构建参数选择。正式截图和录屏中不得出现调试按钮、测试列表、场景选择面板或后台UI。

## 5. 应用层与单一规则源架构

### 5.1 唯一会话适配器

建立或升级唯一 Cocos 应用会话适配器，建议命名为 `ResearchGameplaySession`。职责仅包括：

- 持有 canonical `TimedResearchSession`；
- 接收 Cocos 触摸、有效时间推进、主动开火、动画完成、揭晓完成、暂停、恢复和重开命令；
- 将 Domain 快照转换为不可变 `BattleViewModel` 与 `EffectPlan`；
- 为每次提交、开火、揭晓完成生成稳定唯一 `operationId`；
- 对重复动画回调、连点和前后台恢复进行幂等保护；
- 向表现层暴露可渲染状态，不在表现层重复计算规则。

以下内容必须只由 Domain 决定：

- 路径是否合法；
- 投料份数；
- 处理等级；
- 连线得分、Combo和总分；
- 灵感生成与落点；
- 是否可开火或需要自动开火；
- 料理身份、星级、料理分和首次发现；
- 当前线索索引；
- 正式料理与研究半成品；
- 制作次数；
- 超时结果。

禁止在 `CP0ABattleShell.ts`、`BattleBoardController.ts` 或动画回调中重写上述逻辑。

### 5.2 EffectPlan v2 原地迁移

只保留一份 `assets/game/scripts/application/cp0c/EffectPlan.ts`。删除 `remainingSteps`，不得新增并行的 R1BEffectPlan。

EffectPlan v2 至少包含：

- `operationId`；
- `path`、`flightOrder`；
- `survivorMoves`、`refillMoves`；
- `beforeBoardHash`、`settledBoardHash`、`finalBoardHash`；
- `settledBoard`、`finalBoard`；
- 本次投料 `throwRecord`；
- 当前锅1～6条 `throwRecords`；
- `remainingActiveTimeMs`；
- `linkScoreDelta`、`dishScoreDelta`、`totalScore`；
- `comboCount`、`comboMultiplier`；
- `audioEvent`，仅作为GOOD/GREAT/UNBELIEVABLE确定事件，不播放音频；
- `canFire`、`potFull`、`autoFireReady`；
- 灵感生成、采集和落点信息；
- 是否需要免费确定性洗牌；
- 对应 Domain 快照或稳定快照哈希。

表现层必须消费同一个 EffectPlan，不允许从动画长度反推得分、投料或料理结果。

### 5.3 状态机映射

必须完整映射 canonical `SessionPhase`：

- `READY`；
- `LINKING`；
- `TIMEOUT_GRACE`；
- `ANIMATING`；
- `AUTO_FIRE_READY`；
- `COOKING`；
- `REVEAL`；
- `PARTIAL_RESULT`；
- `SUMMARY`；
- `PAUSED`；
- `SHUFFLING`。

每一时刻只能有一个可交互主状态。输入锁定、计时推进和动画完成回调必须由状态明确控制，不得依赖按钮颜色或节点是否显示来判断规则状态。

## 6. 计时与前后台

### 6.1 有效时间驱动

- Cocos 使用单一有效时间驱动器向 Domain 显式提交毫秒增量；
- 只有 Domain 允许的 `READY` 与 `LINKING` 阶段推进有效时间；
- 不直接把系统墙上时间相减作为剩余时间；
- 动画、烹饪、揭晓、洗牌和暂停期间不得偷偷扣时；
- 帧抖动不得让一次 update 重复扣时；
- 测试使用可注入的 fake clock 或显式 tick，不等待真实90秒；
- 玩家显示采用向上取整的 `MM:SS`，不得出现负数或 `00:60`。

### 6.2 应用切后台

- 监听 Cocos 应用隐藏与恢复事件；
- 进入后台立即暂停并取消未提交路径；
- 恢复后不补扣后台经过的时间；
- 不重复触发连线结算、开火、揭晓或得分；
- 如恢复时正处在动画状态，从同一可恢复节点继续，或使用确定性完成回调收束到唯一结果。

### 6.3 最后10秒

- 计时器切换为珊瑚色柔和警示；
- 只允许视觉节奏提示，本阶段不播放提示音；
- 不使用失败红屏或刺耳闪烁；
- 时间归零后锁定新连线并进入 canonical 超时流程。

## 7. 真实连线、飞锅、掉落与补盘时序

合法连线必须按以下顺序执行：

1. `TOUCH_START` 创建 Domain 路径编辑；
2. `TOUCH_MOVE` 逐格追加或回退；
3. 路径上实时显示连线轨迹与当前格数；
4. `TOUCH_END/CANCEL` 只提交一次；
5. 少于3格由 Domain 取消，棋盘、队列、分数和锅完全无副作用；
6. 合法路径提交后立即锁定新输入；
7. 从 Domain 取得唯一 EffectPlan；
8. 所有被连接食材真实飞向锅，但只融合为1份投料；
9. 飞锅关键节点完成后渲染掉落与补盘；
10. 校验最终可见棋盘 Hash 与 Domain Hash 一致；
11. 渲染本次投料槽、分数跳字、Combo和长连反馈；
12. 如死盘则执行免费确定性洗牌；
13. 如锅未满且时间未归零，恢复输入；
14. 如第6份满锅，进入0.6秒检查反馈后自动开火；
15. 动画完成回调无论被调用几次，都只能推进一次状态。

9连以上总飞锅时间目标约500ms，不得按每个格子串行播放到明显拖慢节奏。飞锅、掉落和补盘期间计时暂停。

## 8. 锅、开火与料理揭晓

### 8.1 投料与开火

- 六槽按实际投料顺序填充；
- 每槽显示食材图标、`1份`和真实处理等级标记；
- 第4份后开火按钮进入已验收的明亮可用态；
- 第4或第5份允许玩家主动开火；
- 第6份停止棋盘输入，显示满锅反馈，然后自动开火；
- 快速连点开火只能生成一个 `cookResultId`；
- 开火不消耗有效时间；
- 锅中图层只显示真实投料食材，不提前出现成品图。

### 8.2 完整揭晓

以下结果播放完整揭晓并暂停计时：

- 任意料理首次发现；
- 珍稀料理；
- 黑暗料理首次发现；
- 玩家从快速揭晓主动展开，本阶段可只预留事件，不增加新按钮。

完整揭晓必须复用已验收的光环、展示台、料理名牌、星星与菜品主构图。点击继续后：

- 只清空锅、六槽、当前锅处理标签和本锅投料序号；
- 保留棋盘、特殊食材、补盘队列位置、总分、Combo、剩余时间、本局发现和制作次数；
- 更新研究线索；
- 返回下一锅。

### 8.3 快速揭晓

- 已发现的普通或特色重复料理使用约1秒快速揭晓；
- 显示成品、菜名、星级、本次料理分和 `本局 ×N`；
- 不重复发首次发现分；
- 结束后自动返回下一锅；
- 快速揭晓不得重新加载场景、重置棋盘或闪白；
- 揭晓完成回调重复触发时不得重复计数或加分。

### 8.4 研究半成品

1～3份超时结果使用轻松、非惩罚性的游戏化小覆盖层：

- 文案可使用 `研究半成品`；
- 显示当前已投入的食材份数；
- 不显示正式菜名、星级或稀有度；
- 不增加任何料理制作次数；
- 已获得连线分保留；
- 确认后进入本局总结。

## 9. 研究线索与一局多菜

### 9.1 显示规则

- 教学第一锅可以显示准确食材图标和份数；
- 未发现料理只显示配置中的食材、风味、颜色、比例或处理提示；
- 不显示未发现料理的成品图、准确菜名和完整答案；
- 已发现料理仍可继续作为线索目标；
- 命中线索只获得 canonical 加分，不影响料理身份和星级；
- 玩家可以忽略线索，自由尝试其他组合。

### 9.2 更新规则

- 开局读取确定性线索队列第一条；
- 每生成一道正式料理，无论命中与否，线索索引只前进一次；
- 完整揭晓点击继续后，下一条线索必须已经准备好；
- 快速揭晓结束后，下一条线索自动显示；
- 线索用尽时按配置化确定性策略循环或停止，不得调用 `Math.random()`；
- 表现层不得硬编码番茄炒蛋提示文字。

### 9.3 最低验证内容

R1-B 默认录屏必须让人看出：

- 第一锅和第二锅是两道不同料理；
- 第一锅结束后顶部研究线索发生变化；
- 第二锅开始时棋盘没有重置；
- 另一个专项录屏证明同一道料理可以重复制作并显示×2。

## 10. 周主题与每日菜单的架构边界

本节是用户已确认的长期内容方向。R1-B 只建立可维护边界和开发配置，不制作正式周主题系统。

### 10.1 目标结构

未来正式内容结构为：

```text
研究季
→ 每周主题菜篮
→ 每日研究菜单
→ 当日食材池、料理池、线索队列和固定/受控棋盘
→ 单局连续制作多锅
→ 永久料理研究地图累计
```

### 10.2 R1-B 必做

建立不依赖Cocos节点的应用层类型和端口，建议名称如下，可按项目现有命名规范调整：

```ts
interface ResolvedResearchMenu {
  contentPackId: string;
  dailyMenuId: string;
  scenarioId: ScenarioId;
  orderId: OrderId;
  ingredientPool: IngredientId[];
  recipePool: RecipeId[];
  clueIds: string[];
}

interface ResearchSchedulePort {
  resolveMenu(input: {
    nowEpochMs: number;
    forcedMenuId?: string;
  }): ResolvedResearchMenu;
}

interface ClockPort {
  nowEpochMs(): number;
}
```

要求：

- 玩家会话必须通过 `ResearchSchedulePort` 获得已解析菜单，表现层不得直接硬编码 `ORD_01` 或番茄炒蛋；
- 建立一个本地 `DevelopmentResearchSchedule`；
- 至少配置 `DEV_MENU_MULTI` → `RS02_MULTI_RECIPE` 和 `DEV_MENU_REPEAT` → `RS01_TUTORIAL_REPEAT`；
- 默认进入 `DEV_MENU_MULTI`；
- 验收时可用隐藏查询参数强制选择菜单；
- 自动测试必须注入固定 `ClockPort`，不得依赖执行测试当天日期；
- canonical Schema `2` / Hash `a35691f9` 必须保持不变；
- 如果新增开发菜单配置文件，必须使用独立 schema/hash，不得偷偷改写 canonical 规则 Hash；
- 不实现远程配置、服务器时间、防改时间、下载资源包或运营后台；
- 不制作第二套周主题美术，不增加第7种食材或第7道料理；
- 不开发料理研究地图UI，历史永久计数仍属于R2。

### 10.3 当前素材范围

为了证明一局可以制作不同料理，R1-B 在既有六道已确认料理范围内，额外允许制作：

- `香葱土豆饼`成品图；
- `田园菌菇汤`成品图；
- 土豆、胡萝卜、蘑菇的锅中模块；
- 如现有素材缺失，允许补充与G1-B一致的GREAT/UNBELIEVABLE贴纸或处理标记。

不得制作：

- 暖锅杂烩正式成品图；
- 黏糊番茄薯团成品图；
- 星辉菌菇蛋盅成品图；
- 新食材、新锅具、周主题背景、研究地图、人物或剧情素材。

新增素材必须为透明背景、模块化、统一左上光源，并在真实Cocos画面检查透明边缘。无法使用imagegen或无法达到G1-B标准时必须停止报告，不得使用Emoji、扁平色块或棋盘图标放大冒充成品料理。

## 11. 商业化架构边界

本阶段只预留接口，不展示商业化UI，不发放真实奖励，不接任何SDK。

### 11.1 R1-B 必做接口

在应用层建立无平台依赖的端口类型，建议至少包含：

```ts
type RewardedPlacementId =
  | 'TIME_EXTENSION'
  | 'SETTLEMENT_DOUBLE'
  | 'CLUE_HINT'
  | 'DAILY_CHEST';

type RewardedAdStatus = 'COMPLETED' | 'CANCELLED' | 'FAILED';

interface RewardedAdPort {
  show(input: {
    placementId: RewardedPlacementId;
    requestId: string;
  }): Promise<{
    status: RewardedAdStatus;
    transactionId?: string;
  }>;
}

interface EntitlementPort {
  has(entitlementId: 'NO_ADS'): boolean;
}
```

要求：

- 接口不得导入 TapTap、穿山甲、优量汇、Unity Ads 或其他广告/支付SDK；
- 不得在R1-B玩家界面出现续时、双倍、提示、宝箱、免广告或付费按钮；
- 不得实际调用端口；
- 不得实际延长时间、翻倍结算或发放货币；
- 不实现商城、金币经济、通行证、广告频控或付费商品；
- 接口只用于防止未来商业化逻辑侵入Domain和表现层；
- 自动测试验证 placement ID、完成/取消/失败状态和无SDK依赖；
- 奖励防重复账本、真实Mock按钮和奖励发放流程留到后续单独授权阶段。

## 12. 视觉保护与动态排版

CP0-R1-A Visual Fix 是不可回退的视觉基线。

### 12.1 必须保留

- 完整49格薄荷绿棋盘；
- 奶油白、珊瑚橙、薄荷绿实体计时/计分HUD；
- 实体研究线索托盘；
- 基础研究锅；
- 一体化2×3木质六槽托盘；
- 大型金色开火按钮；
- 已验收的完整揭晓与快速揭晓层级；
- G1-B果冻玩具厨房、2.5D黏土与软珐琅质感。

禁止把任何区域改回浮空食材、棕色程序框、表格、网页卡片、后台面板或纯文字占位。

### 12.2 动态排版必须验证

- 分数 `0`、`99,999`、`999,999`、`9,999,999` 均不得与标题、边框或Combo重叠；
- 计时 `01:30`、`00:10`、`00:00` 均不裁切；
- Combo `×1.0`、`×1.2`、`×1.5`、`×2.0`、`×3.0` 均能完整显示；
- 六槽内食材图标、`1份`和处理标记不得重叠；
- 长线索必须受最大行数和SHRINK/截断规则控制，不得撑破托盘；
- 快速揭晓中的本次得分和 `本局 ×N` 在两位数计数时仍不重叠；
- 最后10秒的珊瑚色警示不得降低数字可读性。

所有对玩家可见的状态必须由真实ViewModel驱动，不保留R1-A静态数字作为运行时真值。隐藏静态验收入口可保留，但正式默认入口必须进入真实可玩会话。

## 13. GOOD / GREAT / UNBELIEVABLE

- 5～6连显示 `GOOD`；
- 7～8连显示 `GREAT`；
- 9连以上显示 `UNBELIEVABLE`；
- 文案等级只能读取 Domain `audioEvent`；
- 本阶段只实现文字贴纸、缩放、轻粒子与必要震动事件；
- 不接真实英文语音；
- 同一 operationId 不得重复弹出反馈；
- 反馈不得长时间遮挡玩家下一次连线；
- Combo倍率升级可显示短视觉节奏，不播放正式音阶。

## 14. 暂停、重开与总结

### 14.1 暂停

暂停层只提供：

- 继续；
- 重开当前开发菜单；
- 返回R1内部入口。

规则：

- LINKING中暂停取消未提交路径；
- 暂停不扣有效时间；
- 动画和揭晓恢复不得重复回调；
- 返回不保存进行中的研究局；
- 暂停层保持游戏化视觉，不做系统弹窗或网页菜单。

### 14.2 重开

重开必须恢复：

- 该开发菜单固定初盘；
- 固定补盘队列游标；
- 90秒；
- 0分；
- 空锅；
- Combo初始值；
- 本局制作次数0；
- 第一条线索。

不得残留上局 operationId、动画节点、计时器、灵感格或制作计数。

### 14.3 本局总结

本局总结至少显示：

- 本局总分；
- 本局正式料理总数；
- 各料理本局制作次数；
- 新发现料理；
- 最长连线；
- 最高Combo倍率；
- 重新研究；
- 返回R1内部入口。

总结必须是G1-B厨房研究成果板，不得做成表格、排行榜后台或网页统计卡。

不得显示：

- 金币、经验、体力；
- 广告奖励；
- 免广告购买；
- 正式周主题地图进度；
- CP0-R2料理研究地图。

## 15. 自动测试

### 15.1 保留既有测试

以下测试必须全部继续通过，不得删除、跳过、改名逃避或降低断言：

- R001～R024；
- A001～A008；
- RS01～RS05全部6个case；
- CP0-C既有适用测试；
- CP0-R1-A静态视觉测试；
- TypeScript typecheck；
- Cocos Creator 3.8.8 Web Mobile构建。

### 15.2 R1-B 新增测试

至少新增 B101～B124：

| 编号 | 验证内容 |
| --- | --- |
| B101 | Cocos加载canonical Schema 2 / Hash a35691f9 |
| B102 | 默认ResearchSchedule解析为DEV_MENU_MULTI，不在表现层硬编码番茄炒蛋 |
| B103 | 强制DEV_MENU_REPEAT可复现，测试Clock不依赖真实日期 |
| B104 | 真实ViewModel初始为90秒、0分、空锅、第一条线索 |
| B105 | 少于3格提交完全无副作用且不生成EffectPlan |
| B106 | 3、5、7、9连都只渲染1份投料 |
| B107 | EffectPlan v2不含remainingSteps，包含计时、分数、Combo与锅状态 |
| B108 | 飞锅、掉落和补盘完成后的可见棋盘Hash等于Domain Hash |
| B109 | 第4份开火点亮，第6份只触发一次自动开火 |
| B110 | READY/LINKING推进有效时间，其余阶段不推进 |
| B111 | 后台暂停与恢复不补扣时间、不重复结算 |
| B112 | 1秒超时宽限最多提交一次合法路径 |
| B113 | 连线分、料理分和总分按operationId只累计一次 |
| B114 | GOOD/GREAT/UNBELIEVABLE与Domain audioEvent完全一致 |
| B115 | Combo窗口在动画与揭晓期间暂停，倍率只影响连线分 |
| B116 | RS02第一锅土豆饼、第二锅菌菇汤且线索索引推进2次 |
| B117 | RS02两锅之间棋盘、队列、分数和剩余时间连续 |
| B118 | RS01第一次完整揭晓，第二次快速揭晓，本局计数1→2 |
| B119 | 同一cookResultId和揭晓回调不得重复计数或重复首次奖励 |
| B120 | RS05五份超时自动开火与主动开火料理、星级一致 |
| B121 | RS05三份超时只生成半成品且正式料理计数为0 |
| B122 | 重开恢复固定初盘、队列、90秒、0分、空锅和第一条线索 |
| B123 | 分数、计时、Combo、长线索和两位数制作计数动态排版不越界 |
| B124 | 商业化端口无SDK依赖、三种回调状态齐全且玩家界面无商业化入口 |

如果因自动化环境无法可靠像素测试文字越界，B123必须至少验证节点矩形、最大字号/SHRINK策略和极值字符串，并由真实390×844截图补足人工验收。

## 16. 人工验收交付

### 16.1 390×844截图

至少提交以下真实 Cocos 3.8.8 PNG，原始尺寸精确为390×844：

1. `CP0R-R1B-01-Ready-390x844.png`；
2. `CP0R-R1B-02-Link-Good-390x844.png`；
3. `CP0R-R1B-03-Link-Great-Inspiration-390x844.png`；
4. `CP0R-R1B-04-Link-Unbelievable-390x844.png`；
5. `CP0R-R1B-05-Five-Throws-390x844.png`；
6. `CP0R-R1B-06-Full-Pot-Auto-Fire-390x844.png`；
7. `CP0R-R1B-07-Potato-Cake-Reveal-390x844.png`；
8. `CP0R-R1B-08-Next-Clue-Same-Board-390x844.png`；
9. `CP0R-R1B-09-Mushroom-Soup-Reveal-390x844.png`；
10. `CP0R-R1B-10-Repeat-Quick-Reveal-X2-390x844.png`；
11. `CP0R-R1B-11-Timeout-Partial-390x844.png`；
12. `CP0R-R1B-12-Session-Summary-390x844.png`。

截图中不得出现鼠标指针、调试按钮、测试场景名、帧率面板、坐标、后台列表或浏览器开发工具。

### 16.2 一镜到底录屏

必须提交：

1. `CP0R-R1B-V01-Multi-Recipe-390x844.mp4`  
   真实触控完成香葱土豆饼 → 完整揭晓 → 返回同一棋盘 → 线索刷新 → 田园菌菇汤 → 完整揭晓。

2. `CP0R-R1B-V02-Repeat-Recipe-390x844.mp4`  
   真实触控完成两次番茄炒蛋，展示第一次完整揭晓、第二次快速揭晓和本局×2。

3. `CP0R-R1B-V03-Long-Link-Feedback-390x844.mp4`  
   真实触控展示GOOD、GREAT/灵感生成与采集、UNBELIEVABLE。

4. `CP0R-R1B-V04-Timeout-390x844.mp4`  
   分别展示五份锅超时自动开火和三份锅研究半成品；允许使用两个连续、明确分隔的固定子用例，但不得用静态状态跳过规则。

录屏要求：

- 真实Cocos运行；
- 原始390×844；
- 不加速、不剪辑规则过程；
- 不使用可见调试按钮直接跳到结果；
- 每次连线必须由真实触摸/鼠标拖动输入产生；
- 计时暂停与恢复必须在视频中可观察；
- 料理揭晓后必须能看到返回同一棋盘和剩余时间。

### 16.3 性能与原始数据

- Web Mobile基准平均帧率目标≥55 FPS；
- 记录P50、P95帧时间；
- 记录最长单帧；
- 记录至少一次9连飞锅总时长；
- 记录峰值活动飞行节点数；
- 连线期间不得出现明显触摸断裂；
- 提供未经手工改写的性能原始JSON；
- 如设备或采样方式无法支持某指标，必须在报告中明确说明，不得伪造数值。

## 17. 报告与可复现命令

必须新增并实际执行：

- `npm test`；
- `npm run test:unit`；
- `npm run test:scenarios`；
- `npm run test:r0`；
- `npm run test:r1a`；
- `npm run test:r1b`；
- `npm run typecheck`；
- R1-B Cocos Creator 3.8.8 Web Mobile构建证据命令。

交付目录建议为：

```text
reports/cp0-r/r1b/
├── CP0R-R1B-Test-Report.md
├── CP0R-R1B-Test-Report.json
├── CP0R-R1B-Command-Results.json
├── CP0R-R1B-Cocos-Build-Result.json
├── CP0R-R1B-Cocos-Build-3.8.8.log
├── CP0R-R1B-Performance-Raw.json
├── screenshots/
└── videos/
```

报告必须写明：

- 开工基线与完成提交SHA；
- Cocos、Node版本；
- canonical Schema/Hash；
- 新增与修改文件；
- 素材来源、尺寸与Hash；
- 所有测试真实数量与结果；
- 6个canonical场景case结果；
- 4段录屏的尺寸、时长、编码、帧数与Hash；
- 12张截图的尺寸与Hash；
- 性能采样方式和原始数据；
- Domain与canonical配置差异结果；
- 是否存在真实SDK、网络请求、广告或支付入口；
- 已知问题和未完成项；
- Git工作区状态。

构建脚本必须检查真实构建完成标记、必要产物和失败关键字，不能只以Cocos进程退出码宣称成功。

## 18. 明确禁止

R1-B 严禁：

- 修改或复制 canonical Domain 形成第二套规则；
- 修改 canonical Schema `2` / Hash `a35691f9`；
- 使用 `Math.random()` 裁决棋盘、补盘、线索、料理或周菜单；
- 将番茄炒蛋硬编码为唯一可生成料理；
- 用静态ViewModel伪造默认可玩流程；
- 使用自动脚本直接调用结果冒充真实触控录屏；
- 开发正式料理研究地图；
- 写正式localStorage料理历史存档；
- 接入GOOD/GREAT/UNBELIEVABLE真实语音；
- 接入背景音乐或完整音效；
- 接入任何广告、支付、TapTap或统计SDK；
- 展示续时、双倍、宝箱、免广告、商店或付费按钮；
- 实现真实周轮换、远程配置、服务器时间或下载内容包；
- 增加第7种食材、第7道料理或第二种锅具；
- 制作暖锅杂烩、黑暗料理、珍稀料理的正式成品与完整演出；
- 开发人物、剧情、厨房装修、长期成长、活动或通行证；
- 开始CP0-R2、CP0-R3或恢复旧CP0-D；
- 使用网页卡片、后台表格、Emoji、纯色占位或来源不明素材降低视觉标准；
- 删除、跳过或弱化既有测试以获得PASS；
- 改写Git历史、操作既有stash或修改无关文件。

## 19. Git提交与停止条件

建议至少分为：

1. 功能与素材提交：

```text
feat: complete CP0-R1 B playable research loop
```

2. 最终证据归档提交：

```text
test: archive CP0-R1 B acceptance evidence
```

提交并推送到用户指定的现有分支，确认 `origin/main` 与最终HEAD一致，`git status --short`为空。

完成后必须立即停止，只返回：

- 阶段状态；
- 开工基线；
- 实现与证据提交完整SHA；
- 测试和构建结果；
- 截图、录屏、报告、性能原始数据路径；
- 新增素材清单；
- Domain/canonical配置零差异证据；
- 商业化SDK与玩家入口均不存在的证据；
- 已知问题；
- 工作区和推送状态。

不得自行开始CP0-R2，不得因为R1-B通过而制作料理地图、存档、音频、广告或更多周主题内容。

