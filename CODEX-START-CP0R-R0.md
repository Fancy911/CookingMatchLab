# Cooking Match Lab - CP0-R0 Execution Directive

版本：v0.1  
性质：Codex 唯一执行指令草案  
当前状态：等待用户明确授权  
授权范围：仅 CP0-R0 规则、配置与确定性验证  
前置基线：CP0-C-C1 已验收通过，旧 CP0-D 已暂停

> 未收到用户“确认 CP0-R0，授权执行”前不得执行。本任务完成后必须停止，不得开始 CP0-R1、R2、R3，不得恢复旧 CP0-D。

## 1. 开工依据

开工前必须读取并以以下资料为准：

- `docs/02-gameplay/Core-Gameplay-Rules-v1.1.md`，仅作为历史基线；
- `docs/03-content/G2-MVP-Content-v0.2.md`，仅作为食材、料理名称和职责历史基线；
- `docs/04-handoff/G3-Development-Handoff-v0.2.md`，仅保留不冲突的架构与视觉约束；
- 新增并纳入仓库的 `docs/02-gameplay/Core-Gameplay-Rules-v2.0.md`；
- CP0-C-C1 最终实现提交 `73d33dbb60bf4d9aa7c12d07d9ad5458ca1d1035`；
- CP0-C-C1 验收归档提交 `f7c6f94`。

如果 v2.0 正文缺失、内容不完整或与本指令冲突，立即停止并报告，不得根据摘要自行补写。

## 2. 阶段目标

CP0-R0 只验证新核心循环在纯 TypeScript 层是否确定、可配置、无配方冲突且可被固定场景复现。

本阶段必须完成：

1. 一次合法连线固定形成 1 份投料；
2. 4 份可开火、6 份满锅自动开火；
3. 90 秒有效操作计时模型；
4. 单局多锅与重复料理；
5. 连线分、连击倍率和料理得分；
6. GOOD、GREAT、UNBELIEVABLE 确定事件；
7. 灵感食材改为处理标签，不再额外增加份数；
8. 六道 v2 配方；
9. 半盲研究线索的确定性轮换；
10. 料理历史制作次数和幂等结算模型；
11. CP0-C 存档 v1 到 v2 数据迁移纯函数；
12. 配方全量枚举和固定研究局测试。

本阶段不修改 Cocos 页面、不生产素材、不接入真实音频。

## 3. 开工保护

1. 确认工作区干净并记录完整 HEAD SHA。
2. 确认 Cocos Creator 为 3.8.8，Node.js 版本写入报告。
3. 新建 CP0-R0 基线提交或标签并记录完整 SHA。
4. 旧配置哈希 `8737fa94` 只作为 v1 历史记录；v2 修改配置后必须产生新的稳定哈希。
5. 不得为了维持旧哈希而跳过 v2 配置变更。
6. 不得创建第二份 Domain 规则源码。
7. 不得复制一套 v2 规则到根目录 `src` 或临时目录后宣称完成。

## 4. 单一源码与配置迁移

### 4.1 Domain

继续以以下目录作为唯一规则源码：

`assets/game/scripts/domain/cp0b/`

目录名暂时保留属于历史命名，不在本阶段进行无收益的大规模目录重命名。

Domain 继续禁止：

- 导入 `cc`；
- 直接使用 `Math.random()`；
- 读取 Cocos 节点或动画状态；
- 播放音频；
- 写入 localStorage。

### 4.2 Canonical Config

继续使用现有 Cocos resources 配置目录作为唯一正式配置来源：

`assets/resources/game/config/cp0-b/`

允许根据 v2 修改其中的 gameplay、recipes、orders/tutorials 和 scenarios，但禁止另建长期并存的 `cp0-r` 规则配置副本。

配置目录名暂时保留属于历史命名。报告必须说明这是路径兼容，不代表继续使用 v1 规则。

### 4.3 Cocos兼容边界

- 本阶段不要求旧 CP0-C Battle 具备可玩的 v2 UI。
- 必须保证 TypeScript 工程能够通过类型检查和 Cocos 构建冒烟。
- 允许对配置加载适配器和类型接口做最小兼容修改。
- 禁止修改 BattleController、棋盘表现、HUD、锅UI、Reveal UI和场景节点以提前实现 R1。
- 如 Canonical Config 更新导致旧 Battle 无法安全启动，必须在入口明确显示“CP0-R0规则迁移中，视觉接入待R1”的游戏风格阻塞提示，不得白屏；该提示仅作为内部构建保护，不得伪装成 v2 可玩完成。

## 5. v2规则实现口径

### 5.1 连线与投料

- 最低合法连线仍为 3 格。
- 每次合法连线只增加 1 份对应食材。
- 路径格数只影响处理等级、分数与特殊食材生成。
- 无效短连必须保持棋盘、队列、锅、分数、连击、计时状态和 RNG 状态不变。

### 5.2 处理等级

| 格数 | 等级 | AudioEvent |
| ---: | --- | --- |
| 3～4 | NORMAL | 无英文语音 |
| 5～6 | PRECISE | GOOD |
| 7～8 | INSPIRATION | GREAT |
| 9+ | MASTER | UNBELIEVABLE |

音频事件只作为 Domain 输出事件，不在 R0 播放或生成音频文件。

### 5.3 灵感

- 7～8 连生成同类灵感食材，落点继续遵循确定性终点列规则。
- 灵感格在路径中占 1 格。
- 采集灵感格后本次仍只投料 1 份。
- 当前锅新增 `INSPIRATION` 标签。
- 旧版“灵感格入锅算2单位”必须从规则、配置、提示和测试中删除。

### 5.4 锅

- 最低开火份数：4；
- 最大容量：6；
- 第4、5份后可主动开火；
- 第6份提交后产生 `POT_FULL_AUTO_FIRE_READY` 事件；
- 0.6秒属于表现层默认延迟，不进入 Domain 真值计时；
- Domain 在收到确认动作后执行满锅自动开火，避免测试依赖真实 setTimeout。

### 5.5 有效操作计时

- 默认 90,000ms；
- Domain 使用显式 `advanceActiveTime(ms)` 或等价可测试接口；
- 禁止在规则层直接读取 `Date.now()`、`performance.now()` 或真实定时器；
- READY、LINKING阶段可以推进有效时间；
- 动画、烹饪、揭晓、暂停和系统洗牌状态不得消耗有效时间；
- 归零时 4～6份锅进入自动开火流程；
- 1～3份锅产生研究半成品结果；
- 空锅直接进入会话总结；
- 当前连线的1秒宽限必须由显式状态和测试时钟控制。

### 5.6 连线分

严格按 v2.0 正文实现：

```text
基础分 = 路径格数 × 10
长度奖励：3～4=0、5～6=30、7～8=80、9+=150
灵感格奖励 = 50
连线分 = round((基础分 + 长度奖励 + 灵感格奖励) × 连击倍率)
```

连击倍率：

- 1～2次：1.0；
- 3～4次：1.2；
- 5～6次：1.5；
- 7～9次：2.0；
- 10次及以上：3.0。

连击窗口默认3000ms有效操作时间。倍率只影响连线分。

### 5.7 六道配方

| 料理 | 份数组合 | 条件 |
| --- | --- | --- |
| 番茄炒蛋 | 番茄2、鸡蛋2、香葱1 | 无 |
| 香葱土豆饼 | 土豆3、鸡蛋1、香葱1 | 无 |
| 田园菌菇汤 | 蘑菇2、胡萝卜1、土豆1、香葱1 | 无 |
| 暖锅杂烩 | 任意4～6份且未命中明确料理 | 兜底 |
| 黏糊番茄薯团 | 番茄2、土豆2、蘑菇1 | 黑暗标签 |
| 星辉菌菇蛋盅 | 蘑菇3、鸡蛋2 | 必须INSPIRATION |

继续沿用既有料理 ID，禁止增加第7道料理。

### 5.8 星级与料理得分

- 明确料理配方准确分100；暖锅杂烩50。
- 单次处理分：3～4格60、5～6格80、7～8格90、9+格100；采集灵感格+10，最高100。
- 料理质量总分 = 准确分×60% + 平均处理分×40%。
- 3星：90～100；2星：70～89.99；1星：0～69.99。
- 星级不得读取研究线索是否匹配。
- 暖锅杂烩最高70分，最多2星。

料理得分严格读取配置：普通500、特色750、珍稀1200、黑暗标签+200、每星+200、首次发现+500、命中研究线索+300。

### 5.9 多锅与线索

- 正式料理揭晓完成后只清锅，不重置棋盘、队列、总分、连击和剩余有效时间。
- 每完成一道正式料理都推进到下一条研究线索，无论是否命中当前线索。
- 重复料理允许正常生成和计分。
- 线索命中只影响额外料理分，不改变菜名、星级和稀有度。

### 5.10 料理计数模型

纯规则层新增：

- 本局每道料理制作次数；
- 历史累计制作次数模型；
- 最高星级；
- 最高料理得分；
- 唯一 `cookResultId`；
- 有限的已处理ID去重集合或队列。

同一 `cookResultId` 重复提交不得二次增加次数、分数或发现记录。

### 5.11 存档迁移纯函数

实现并测试 v1→v2 纯数据迁移，但本阶段不接 `sys.localStorage`。

迁移必须保留：

- 已发现番茄炒蛋；
- 已发现暖锅杂烩；
- 两道料理已有最高星级。

新增字段使用安全默认值：制作次数0、最高料理得分0、灵感提示false、音乐/音效/语音默认开启。

## 6. 配方枚举

必须枚举六种食材在总份数4、5、6下的全部非负整数组合，并分别组合以下标签状态：

- 无标签；
- INSPIRATION；
- MASTER；
- INSPIRATION+MASTER。

必须证明：

1. 每个输入只生成一个确定料理；
2. 五道明确配方之间没有重叠；
3. 星辉菌菇蛋盅没有INSPIRATION时不能成立；
4. 未命中明确配方时总是进入暖锅杂烩；
5. 任意合法4～6份输入不会返回空结果；
6. 同输入重复运行快照哈希一致。

输出枚举摘要JSON和Markdown，不需要列出所有组合正文，但必须包含总组合数、各料理命中数、冲突数和无结果数。

## 7. 固定研究局场景

新增至少五个v2固定场景。可以由确定性搜索工具寻找可行棋盘与逐列补充队列，但最终必须固化为完整JSON，运行时不得调用搜索器。

### RS01_TUTORIAL_REPEAT

- 90秒；
- 第一锅番茄炒蛋；
- 第二锅再次制作番茄炒蛋；
- 验证同菜重复、计数从1到2、首次发现奖励只发一次。

### RS02_MULTI_RECIPE

- 90秒；
- 依次生成香葱土豆饼、田园菌菇汤；
- 验证多锅后棋盘和队列继续、线索每锅推进。

### RS03_DARK

- 90秒；
- 生成黏糊番茄薯团；
- 随后继续生成一道正常料理；
- 验证黑暗标签、黑暗加分和制作计数。

### RS04_INSPIRATION

- 90秒；
- 先通过7连生成灵感蘑菇；
- 采集灵感蘑菇，该次仍只增加1份蘑菇；
- 最终以蘑菇3、鸡蛋2和INSPIRATION生成星辉菌菇蛋盅；
- 随后继续下一锅。

### RS05_TIMER_END

包含两个子用例：

- 当前锅5份且时间归零，自动开火，结果与主动开火一致；
- 当前锅3份且时间归零，只生成研究半成品，不增加料理制作次数。

每个固定场景必须包含：

- 7×7初始棋盘；
- 逐列固定补充队列；
- 研究线索队列；
- 显式有效时间推进脚本；
- 推荐连线路径；
- 每次操作后锅中份数、剩余时间、连击、分数和关键标签；
- 每锅结果；
- 最终本局计数与总分；
- 预期稳定快照哈希。

不得通过直接注入锅中结果、直接调用料理ID或跳过棋盘连线来伪造场景通过。

## 8. 自动测试最低要求

新增 R001～R024：

| 编号 | 验证内容 |
| --- | --- |
| R001 | 3/5/7/9连均只增加1份 |
| R002 | 无效短连无任何真值副作用 |
| R003 | 4份可开火、6份进入满锅自动开火准备 |
| R004 | 90秒显式有效计时与状态限制 |
| R005 | 归零时当前连线1秒宽限 |
| R006 | 四档连线分与AudioEvent正确 |
| R007 | 连击窗口和倍率边界 |
| R008 | 演出状态不消耗有效时间 |
| R009 | 灵感格只投1份并写入INSPIRATION |
| R010 | 六道配方标准输入正确 |
| R011 | 4～6份全量枚举无重叠、无空结果 |
| R012 | 星级公式和暖锅最高2星 |
| R013 | 研究线索不改变料理身份和星级 |
| R014 | 同菜一局重复生成并正常计数 |
| R015 | cookResultId幂等防重复 |
| R016 | 多锅只清锅并保留保护状态 |
| R017 | 每道正式料理后线索推进 |
| R018 | 1～3份超时生成半成品且不计数 |
| R019 | 自动开火与主动开火结果一致 |
| R020 | 同配置、seed、时间脚本和操作得到同hash |
| R021 | 缺字段、非法配方和非法时间明确失败 |
| R022 | Domain无cc导入且规则无Math.random |
| R023 | v1存档迁移保留发现与最高星级 |
| R024 | GOOD/GREAT/UNBELIEVABLE事件不重复发出 |

固定场景测试编号为 RS01～RS05；允许按每锅和子用例拆成更多断言，但报告必须能映射回五个场景。

## 9. 历史测试处理

- 不得删除 CP0-B、CP0-C 的历史报告和录屏。
- v1 中依赖“每格1单位、三投料、步数制”的测试已被产品规则正式替代，可以标记为 `SUPERSEDED_BY_V2`，不得伪称仍然PASS。
- 与v2不冲突的路径、确定补盘、稳定哈希、配方优先级、无cc导入等测试必须继续保留或迁移。
- 测试报告必须列出哪些旧测试继续通过、哪些因规则变更被替代、对应的新测试编号是什么。

## 10. 命令与质量门槛

至少执行：

```bash
npm test
npm run test:unit
npm run test:scenarios
npm run typecheck
```

如需新增 `test:r0`、枚举报告或场景报告命令，应写入package.json并在总测试中执行。

还必须执行：

- Cocos Creator 3.8.8 Web Mobile构建冒烟；
- 规则目录无`cc`导入扫描；
- 规则代码无`Math.random()`扫描；
- 单一规则源码扫描；
- 配置哈希生成和重复运行一致性检查。

## 11. 本阶段交付物

1. `docs/02-gameplay/Core-Gameplay-Rules-v2.0.md`；
2. v2 canonical config；
3. Domain v2规则实现；
4. 五个固定研究局JSON；
5. R001～R024测试；
6. 配方枚举报告JSON和Markdown；
7. 固定场景测试报告JSON和Markdown；
8. CP0-R0总报告JSON和Markdown；
9. 一张390×844 Cocos构建冒烟截图。

建议路径：

- `reports/cp0-r/r0/CP0R-R0-Test-Report.md`；
- `reports/cp0-r/r0/CP0R-R0-Test-Report.json`；
- `reports/cp0-r/r0/CP0R-R0-Recipe-Enumeration.md`；
- `reports/cp0-r/r0/CP0R-R0-Recipe-Enumeration.json`；
- `reports/cp0-r/r0/CP0R-R0-Scenario-Report.md`；
- `reports/cp0-r/r0/CP0R-R0-Scenario-Report.json`；
- `reports/cp0-r/r0/CP0R-R0-Cocos-Smoke-390x844.png`。

冒烟截图只证明工程没有白屏或构建错误，不代表v2 UI完成。截图中不得出现网页错误页、后台面板或虚假的v2功能完成状态。

## 12. 明确禁止

本阶段不得：

- 修改Battle UI布局；
- 把步数HUD改成正式计时HUD；
- 制作六投料槽；
- 实现运行中的Tween、粒子或分数跳字；
- 生产或接入GOOD/GREAT/UNBELIEVABLE音频文件；
- 制作料理研究地图界面；
- 生产新料理和地图美术；
- 实现正式存档读写；
- 开发广告、内购、TapTap SDK；
- 增加食材、料理、锅具、人物、剧情或活动；
- 执行CP0-R1、R2、R3；
- 恢复或继续旧CP0-D-D0。

## 13. Git与停止条件

1. 不覆盖用户无关改动，不改写历史。
2. 将规则、配置、测试、固定场景和报告纳入一次明确提交。
3. 建议提交信息：`feat: complete CP0-R0 timed multi-cook rules`。
4. 推送当前远程分支并返回完整SHA。
5. 提交后工作区必须干净。
6. 完成后立即停止，只交付：
   - 开工基线和完成SHA；
   - 新配置哈希；
   - 变更文件；
   - R001～R024结果；
   - RS01～RS05结果；
   - 配方枚举摘要；
   - 历史测试迁移表；
   - 四条命令与Cocos构建结果；
   - 报告和390×844冒烟截图；
   - 阻塞项、已知问题和工作区状态。

不得开始CP0-R1，等待用户验收与明确授权。
