# 《料理消消研究所》CP0-R1 开发交接与验收方案 v0.1

日期：2026-08-02  
当前性质：待用户确认的开发交接稿  
前置基线：CP0-R0 已正式验收通过  
Git 基线：`a2e662883d73062b57e05a8d311543c06ab24762`  
配置 Schema/Hash：`2 / a35691f9`  
Cocos Creator：3.8.8  
视觉方向：G1-B「果冻玩具厨房」

## 1. 阶段目标

CP0-R1 将 CP0-R0 已通过的纯 TypeScript v2 规则接入 Cocos，形成第一版可以真实试玩的新核心循环：

```text
90秒有效操作时间
→ 连线消除并显示分数/连击反馈
→ 每次合法连线形成1份投料
→ 4份后可开火，6份满锅自动开火
→ 料理揭晓并计入本局次数
→ 清锅但保留棋盘和时间
→ 更新研究线索并继续下一锅
→ 时间结束自动处理当前锅
→ 显示本局总结
```

CP0-R1 不制作料理研究地图，不接正式音效和语音文件，不做持久化正式存档；这些属于 CP0-R2。

## 2. 为什么继续拆分

新 HUD 比 CP0-C 增加计时、分数、连击、研究线索和六份投料记录，原有三槽布局必须重构。为避免功能接完后才发现画面拥挤，R1 分为两个检查点：

| 检查点 | 范围 | 完成后 |
| --- | --- | --- |
| CP0-R1-A | 新对局视觉壳与三张静态状态图 | 必须停止并等待UI验收 |
| CP0-R1-B | 规则接入、90秒、多锅、分数连击、快速揭晓、超时结算 | 必须停止并等待完整试玩验收 |

R1-A 未通过前不得开始 R1-B。

## 3. CP0-R1-A：视觉壳

### 3.1 只展示三个静态状态

1. `READY`：90秒、0分、研究线索、空锅、六个空投料位；
2. `POT_REVIEW`：番茄2份、鸡蛋2份、香葱1份，开火可用，同时展示分数、连击与GOOD反馈位置；
3. `QUICK_REVEAL_REPEAT`：重复番茄炒蛋快速揭晓，显示本次得分、星级、累计制作×2和下一条线索提示。

使用键盘1/2/3、查询参数或隐藏调试入口切换。玩家截图不得出现调试按钮、后台列表、坐标或测试文本。

### 3.2 390×844布局

| 区域 | 建议范围 | 要求 |
| --- | --- | --- |
| 暂停 | x14–58，y46–90 | 44×44最小触控区 |
| 计时器 | x72–184，y46–96 | 厨房计时器实体造型，显示01:30 |
| 分数 | x196–374，y46–96 | 实体记分牌，数字优先，不做网页统计卡 |
| 研究线索 | x16–374，y104–170 | 奶油实体托盘，最多三行短线索 |
| 棋盘 | x13–377，y178–542 | 保持完整7×7和可触摸尺寸 |
| 锅 | x48–342，y548–692 | 继续使用基础研究锅分层素材 |
| 六份投料 | x14–274，y700–810 | 同一块砧板中的2×3凹槽，不做六张卡片 |
| 开火 | x286–374，y712–800 | 第4份后高亮 |

底部安全区和不同全面屏适配必须通过 SafeArea 或等价布局策略处理，不能只为390×844硬贴坐标。

### 3.3 HUD规则

- 原“剩余步数”完全移除，不得同时出现步数和倒计时；
- 计时显示 `01:30`，最后10秒预留珊瑚色警示；
- 分数使用易扫读大数字，如 `12,480`；
- 连击倍率以小型贴纸或漂浮徽章显示，例如 `COMBO ×1.5`；
- GOOD/GREAT/UNBELIEVABLE位于棋盘中上部，不遮挡连线路径；
- 研究线索不显示未发现料理成品图和准确答案；
- 教学第一锅允许显示番茄、鸡蛋、香葱三种份数提示。

### 3.4 六份投料区

- 使用一块实体木质或软珐琅砧板；
- 六个槽位为2行×3列；
- 每个槽位显示食材小图、`1份`和处理等级小标记；
- 空槽只显示凹槽，不显示横杠文字；
- 不能把六份缩成难以识别的小圆点；
- 第4份加入后开火按钮明显点亮；
- 第6份状态预留满锅金色边光。

### 3.5 快速揭晓

- 已发现重复料理使用约1秒的紧凑揭晓构图；
- 继续使用厨房背景压暗，不跳到网页式结算页；
- 必须显示料理成品、菜名、星级、本次料理分、`累计制作 ×2`；
- “下一条研究线索”只显示一句简短预告；
- 首次发现完整揭晓仍保留现有大舞台构图，R1-A只展示重复料理快速揭晓。

### 3.6 R1-A素材白名单

允许新增：

- 香葱棋盘图标；
- 香葱锅中原料模块；
- 90秒厨房计时器；
- 实体分数牌；
- 六槽投料砧板；
- 普通/精切/灵感/大师四档小标记；
- GOOD/GREAT/UNBELIEVABLE文字贴纸；
- 快速揭晓框、得分条和累计次数印章。

优先复用：厨房背景、棋盘、番茄/鸡蛋素材、基础研究锅、开火按钮、番茄炒蛋、星星、普通徽章。

禁止提前制作其他五道料理、料理地图、黑暗/珍稀完整演出和音频文件。

### 3.7 R1-A交付

三张真实 Cocos 390×844 PNG：

- `CP0R-R1A-01-Ready-390x844.png`；
- `CP0R-R1A-02-Pot-Review-390x844.png`；
- `CP0R-R1A-03-Quick-Reveal-Repeat-390x844.png`。

提供10～20秒录屏展示隐藏状态切换，但玩家画面不得出现调试控件。

## 4. CP0-R1-B：可玩循环

R1-B 仅在 R1-A 验收通过后授权。

### 4.1 默认可玩场景

默认接入 `RS01_TUTORIAL_REPEAT`：

- 第一锅制作番茄炒蛋；
- 第一次播放完整揭晓并记录本局制作1次；
- 返回同一棋盘和剩余时间；
- 第二锅再次制作番茄炒蛋；
- 第二次播放快速揭晓并显示累计制作×2；
- 首次发现奖励只发一次；
- 同一锅配方为番茄2份、鸡蛋2份、香葱1份。

隐藏验收入口接入 `RS05_TIMER_END` 的两个用例，用于自动开火和半成品结算，不进入正式玩家选择页。

### 4.2 应用层职责

建立唯一 Cocos 应用会话适配器，名称可为 `ResearchGameplaySession`：

- 持有 canonical `TimedResearchSession`；
- 接受Cocos触摸、有效时间推进、开火、揭晓继续和暂停命令；
- 将 Domain 快照转换为不可变 ViewModel 和 EffectPlan；
- 不在表现层计算配方、星级、分数、连击、制作次数或超时结果；
- 每个提交和开火动作必须带唯一 operationId，动画完成回调幂等。

### 4.3 EffectPlan v2

删除旧 `remainingSteps` 等v1字段，至少包含：

- operationId；
- 路径与飞行顺序；
- settled/final board；
- 当前投料1份及处理等级；
- 当前锅1～6份记录；
- remainingActiveTimeMs；
- linkScoreDelta和totalScore；
- comboCount和comboMultiplier；
- AudioEvent（只作为事件，不播放音频）；
- canFire、potFull、autoFireReady；
- inspiration spawn/collected信息。

### 4.4 计时

- Cocos只在Domain允许的READY/LINKING阶段调用显式有效时间推进；
- ANIMATING、COOKING、REVEAL、SHUFFLING和PAUSED不推进；
- 后台切换时暂停；
- 恢复时不补扣后台时间；
- 归零时进入1秒当前连线宽限；
- 4～6份自动开火，1～3份生成半成品，空锅直接总结；
- 表现层不得自行根据UI数字决定结果。

### 4.5 分数与连击表现

- 每次合法连线显示分数跳字；
- 5～6连显示GOOD；
- 7～8连显示GREAT；
- 9连以上显示UNBELIEVABLE；
- 文字等级读取Domain AudioEvent；
- 本阶段只展示文字、缩放、粒子和轻震，不接真实语音；
- 连击在3秒有效操作时间内延续，动画期间不掉；
- 暂停恢复不得重复加分或重复弹字。

### 4.6 多锅与揭晓

- 首次番茄炒蛋完整揭晓；
- 点击继续后只清锅和六份投料区；
- 棋盘、队列、总分、连击、时间和线索进度保持；
- 第二次番茄炒蛋快速揭晓；
- 快速揭晓约1秒后自动返回下一锅；
- 本阶段历史累计只保存在当前运行内存，不写正式localStorage；
- 正式持久化与料理地图在R2完成。

### 4.7 本局总结

时间结束后显示游戏化简版总结：

- 本局总分；
- 本局制作料理总数；
- 番茄炒蛋本局制作次数；
- 最长连线；
- 最高连击倍率；
- 重新试玩、返回R1内部入口。

不得显示金币、经验、广告奖励或正式地图进度。

### 4.8 暂停

暂停层提供：继续、重开当前场景、返回内部入口。

- LINKING暂停取消未提交路径；
- 动画和揭晓暂停后从同一进度恢复；
- 不重复执行回调；
- 重开恢复固定初盘、90秒、0分和空锅；
- 返回不保存进行中的研究局。

## 5. 自动测试

保留 R001～R024、A001～A008和RS01～RS05。

R1新增 C101～C118：

| 编号 | 内容 |
| --- | --- |
| C101 | Cocos加载schema2和哈希a35691f9 |
| C102 | RS01初始ViewModel正确 |
| C103 | 合法连线一次只渲染1份投料 |
| C104 | EffectPlan不含remainingSteps |
| C105 | 4份开火点亮、6份自动开火事件 |
| C106 | READY/LINKING才推进时间 |
| C107 | 动画、烹饪、揭晓、暂停不推进时间 |
| C108 | 1秒超时宽限只提交一次 |
| C109 | 连线分和总分只累计一次 |
| C110 | GOOD/GREAT/UNBELIEVABLE与Domain一致 |
| C111 | 连击在动画暂停期间不掉 |
| C112 | 第一锅完整揭晓并计数1 |
| C113 | 继续后保护棋盘、队列、时间和总分 |
| C114 | 重复料理快速揭晓并计数2 |
| C115 | 5份超时自动开火与主动开火一致 |
| C116 | 3份超时只生成半成品且不计数 |
| C117 | 暂停/前后台恢复不重复回调 |
| C118 | 重开恢复固定初始状态 |

## 6. R1-B人工验收

### 6.1 截图

至少交付：

- READY；
- 5连GOOD；
- 7连GREAT/灵感；
- 9连UNBELIEVABLE；
- 五份锅检查；
- 六份满锅；
- 首次完整揭晓；
- 重复料理快速揭晓；
- 超时自动开火；
- 研究半成品；
- 本局总结。

全部为真实 Cocos 3.8.8、390×844 PNG。

### 6.2 录屏

- V01：RS01从空锅到第一锅完整揭晓，再到第二锅快速揭晓；
- V02：六份满锅自动开火；
- V03：时间结束自动开火与三份半成品两个用例。

录屏必须真实触控、一镜到底、不加速，不使用可见调试按钮直接跳过规则。

### 6.3 性能

- Web Mobile基准平均帧率目标≥55 FPS；
- 连线、飞锅和补盘不得出现明显触摸断裂；
- 9连以上总飞锅时间继续控制在约500ms；
- 记录至少一段原始性能采样数据。

## 7. R1全阶段禁止

- 不开发料理研究地图；
- 不写正式料理历史存档；
- 不接GOOD/GREAT/UNBELIEVABLE真实语音；
- 不接正式背景音乐和完整音效；
- 不接黑暗、珍稀完整演出；
- 不实现其他四个研究局的玩家入口；
- 不增加食材、料理、锅具、人物、剧情、成长、广告、内购或TapTap SDK；
- 不开始CP0-R2、R3或恢复旧CP0-D。

## 8. Git与停止

R1-A和R1-B分别提交、推送和验收。任何检查点完成后必须工作区干净并停止。不得用一个提交跨越两个检查点。

当前只准备R1-A唯一执行指令；尚未授权任何代码或素材生产。
