# Cooking Match Lab - CP0-D-D0 Execution Directive

版本：v0.1  
性质：Codex 唯一执行指令  
授权范围：仅 CP0-D-D0  
前置阶段：CP0-C-C1 已验收通过

> 完成 CP0-D-D0 的验收材料并提交后必须停止。不得开始 CP0-D-D1、D2、D3，不得开始 CP1 或其他后续内容。

## 1. 开工基线

1. 在仓库 `Fancy911/CookingMatchLab` 当前主开发分支工作。
2. 开工前确认：
   - CP0-C-C1 最终实现提交存在：`73d33dbb60bf4d9aa7c12d07d9ad5458ca1d1035`；
   - CP0-C-C1 验收归档提交存在：`f7c6f94`；
   - 工作区干净；
   - Cocos Creator 精确版本为 `3.8.8`；
   - Node.js 版本记录在报告中；
   - 配置哈希仍为 `8737fa94`。
3. 如果仓库状态、冻结文档、Cocos 版本、既有素材或配置与上述信息不一致，立即停止并报告，不得自行猜测或重建。
4. 开工前建立明确的 D0 基线提交或标签，并将完整 SHA 写入报告。

## 2. D0 唯一目标

在不复制规则源码、不修改冻结玩法数值的前提下，把 CP0-C 仅支持 O1 的运行闭环扩展为以下三个可从 PrototypeLab 选择并真实游玩的固定场景：

- `O1_TUTORIAL_001`；
- `O2_STANDARD`；
- `O3_STANDARD`。

本阶段只完成：

- 多场景运行架构；
- PrototypeLab 内部选择页；
- O2_STANDARD 标准完成香葱土豆饼；
- O3_STANDARD 标准完成田园菌菇汤；
- 存档 v1 → v2 兼容升级；
- 暂停、重开当前场景、返回 PrototypeLab；
- 对应测试、截图、录屏和报告。

## 3. 架构要求

### 3.1 单一规则源码

继续只使用以下目录作为规则层唯一源码：

`assets/game/scripts/domain/cp0b/`

禁止：

- 恢复根目录 `src/cp0b`；
- 在表现层复制配方、星级、掉落、补盘或订单判定；
- 为 O2、O3 各写一套规则；
- 通过 UI 脚本硬编码正确料理结果。

规则层继续禁止导入 `cc`。

### 3.2 场景参数化

改造 `PrototypeSession`：

- 构造时显式接收 `scenarioId`；
- 禁止继续写死 `O1_TUTORIAL_001`；
- `restart()` 必须重启当前场景，而不是固定回到 O1；
- 当前场景 ID 必须可以被控制器和测试读取；
- 五个冻结场景继续使用同一个 `PrototypeSession`、`OrderSession`、配置注册表和 Battle 表现层。

### 3.3 配置不变

必须直接读取现有冻结配置：

- gameplay；
- ingredients；
- recipes；
- orders；
- tutorials；
- 五个固定 scenario JSON。

不得修改：

- 配方数量范围；
- 配方比例；
- 配方优先级；
- 星级公式；
- 初始步数；
- 初始棋盘；
- 固定补充队列；
- 推荐路径；
- 灵感生成规则；
- 配置哈希 `8737fa94`。

如发现现有配置与 G2 v0.2 不一致，停止并报告，不得自行修正数值。

## 4. PrototypeLab

建立内部使用的 PrototypeLab 入口，必须提供：

- O1 教学按钮；
- O2 标准按钮；
- O2 黑暗按钮；
- O3 标准按钮；
- O3 灵感按钮；
- 重置原型存档；
- 当前配置哈希；
- 简短的内部测试说明。

D0 阶段：

- O1、O2_STANDARD、O3_STANDARD 可以进入；
- O2_BLACK 和 O3_INSPIRATION 显示“后续检查点”，不可进入；
- 不得伪装成已完成；
- 不得通过 URL 参数作为玩家唯一入口。

PrototypeLab 虽然是内部页面，也必须沿用 G1-B“果冻玩具厨房”风格：

- 使用厨房中的实体托盘、菜谱夹、木牌或玩具按钮；
- 禁止网页列表、管理后台、表格界面和纯文字卡片堆叠；
- 禁止 Emoji 和扁平色块占位。

PrototypeLab 不作为正式游戏首页，不增加货币、任务、活动或商业入口。

## 5. 三个场景的运行要求

### 5.1 O1_TUTORIAL_001

必须保持 CP0-C 现有行为和视觉，不得回归：

- 番茄 5；
- 鸡蛋 4；
- 主动开火；
- 生成番茄炒蛋；
- 剩余 5 步。

### 5.2 O2_STANDARD

必须按照冻结配置真实完成：

- 土豆 6；
- 鸡蛋 3；
- 香葱 3；
- 三次投料后不自动开火；
- 玩家主动开火；
- 生成香葱土豆饼；
- 订单成功；
- 最终剩余 5 步。

订单托盘显示：

- 香葱土豆饼；
- 土豆 5～7；
- 鸡蛋 3～4；
- 香葱 3～4。

### 5.3 O3_STANDARD

必须按照冻结配置真实完成：

- 蘑菇 5；
- 胡萝卜 4；
- 土豆 3；
- 玩家主动开火；
- 生成田园菌菇汤；
- 订单成功；
- 最终剩余 5 步。

订单托盘不能直接显示隐藏答案和精确配方，应按照 G2 v0.2 显示研究线索：

- 菌香是主角；
- 需要一种清甜根茎和一种绵密根茎；
- 菌菇的量接近另外两种食材总量，但不要超过它们。

必须保持信息可读，不能让三行线索挤压棋盘或破坏 390×844 布局。

## 6. 存档升级

将 C1 存档升级为版本化 v2。v2 至少支持：

- 六道料理的发现记录；
- 六道料理各自最高星级；
- `inspirationUnitHintShown`；
- `schemaVersion`。

必须满足：

1. C1 v1 中已有的番茄炒蛋和暖锅杂烩记录不能丢失；
2. 提供 v1 → v2 自动迁移；
3. 旧存档迁移后写入 v2；
4. 非法或损坏存档保留原字符串备份并建立新存档；
5. 最高星级只能提高，不能降低；
6. 重开测试场景不得清空发现记录；
7. “重置原型存档”必须明确二次确认；
8. 不保存进行中的棋盘、锅、步数和队列；
9. 不把 PrototypeLab 最后选择的场景写入正式存档数据；
10. 不接账号、云存档或服务器。

必须为存档迁移和损坏恢复增加自动测试。

## 7. 暂停和导航

Battle 暂停层必须提供：

- 继续；
- 重开当前测试场景；
- 返回 PrototypeLab。

要求：

- 重开后恢复对应固定初盘、完整步数和队列；
- 不得重开成 O1；
- 返回 PrototypeLab 不保存进行中的订单；
- 已完成揭晓并写入的发现记录保留；
- 连线过程中暂停时取消未提交路径，不扣步；
- 动画、结算和保存不得因暂停恢复重复执行。

## 8. D0 素材白名单

本次只允许新增实际需要的素材：

- 香葱普通棋盘图标；
- 土豆、胡萝卜、蘑菇、香葱锅中原料模块；
- 香葱土豆饼成品；
- 田园菌菇汤成品；
- 特色料理徽章及特色揭晓所需基础装饰；
- PrototypeLab 所需实体厨房按钮、标题和托盘组件。

素材必须符合 G1-B：

- 果冻玩具厨房；
- 2.5D 黏土与软珐琅质感；
- 左上方统一光源；
- 成品料理应明显比棋盘图标更湿润、更有食欲；
- UI 必须像厨房中的实体器具。

如果使用 imagegen 生成素材：

- 必须保留透明背景；
- 必须拆成可独立使用的模块；
- 不得把文字、按钮或完整页面烘焙进背景；
- 生成后必须在 Cocos 真实画面中检查边缘、比例和透明通道。

如果无法生成达到要求的素材，停止并报告。不得用 Emoji、扁平色块、网页组件、无关免费图标或文字占位代替后宣称完成。

本次禁止制作：

- 黏糊番茄薯团；
- 星辉菌菇蛋盅；
- 灵感食材覆盖层和灵感教学图；
- 黑暗料理烟雾和标签；
- 珍稀徽章和金色星屑；
- 正式图鉴和正式首页；
- Android 商店图标；
- 后续阶段素材。

## 9. 视觉与布局要求

基准验收尺寸为 390×844。

必须保持：

- 顶部 HUD、订单托盘、棋盘、锅、投料托盘和开火按钮不互相遮挡；
- 棋盘仍是完整 7×7；
- O2 三种建议用量清晰但不过度拥挤；
- O3 线索可读；
- 香葱图标与胡萝卜、蘑菇在颜色和轮廓上明显不同；
- 三次投料均在投料槽中显示食材、连接格数和实际单位；
- 特色料理揭晓比普通料理更丰富，但不得使用 D1 黑暗或 D2 珍稀素材；
- 所有玩家截图中不得出现调试按钮、坐标、路径脚本或测试日志。

不得为了塞入信息而缩小棋盘格到难以触摸。

## 10. 自动测试

保留并运行全部既有测试，同时增加 D0 测试，至少验证：

1. 构造会话时正确加载指定场景；
2. 非法场景 ID 明确失败；
3. O2_STANDARD 固定路径得到香葱土豆饼和 5 步；
4. O3_STANDARD 固定路径得到田园菌菇汤和 5 步；
5. `restart()` 保持当前 `scenarioId`；
6. 重开后初盘、步数和队列游标恢复；
7. 返回 PrototypeLab 不写入进行中订单；
8. C1 v1 存档正确迁移到 v2；
9. 六道料理 ID 均可被 v2 存档接受；
10. 最高星级不下降；
11. 损坏存档被备份并恢复；
12. 配置哈希仍为 `8737fa94`；
13. 表现层没有第二套配方判定；
14. Domain 层仍无 `cc` 导入；
15. 禁止使用 `Math.random()` 影响固定场景；
16. O1 现有 C1 测试无回归。

执行并报告：

```bash
npm test
npm run test:unit
npm run test:scenarios
npm run typecheck
```

如工程已有 C1 专项命令，也必须继续执行。

## 11. 人工验收材料

必须提供真实 Cocos Creator 3.8.8 运行结果。

### 11.1 截图

全部原始尺寸精确为 390×844 PNG：

1. `CP0D-D0-01-PrototypeLab-390x844.png`；
2. `CP0D-D0-02-O2-Battle-Ready-390x844.png`；
3. `CP0D-D0-03-O2-Pot-Review-390x844.png`；
4. `CP0D-D0-04-Scallion-Potato-Cake-Reveal-390x844.png`；
5. `CP0D-D0-05-O3-Research-Clues-390x844.png`；
6. `CP0D-D0-06-Garden-Mushroom-Soup-Reveal-390x844.png`。

### 11.2 录屏

提供两段一镜到底录屏：

- `CP0D-D0-V01-O2-Standard-390x844.mp4`；
- `CP0D-D0-V02-O3-Standard-390x844.mp4`。

每段必须从 PrototypeLab 选择场景开始，完整展示：

- 进入 Battle；
- 玩家真实触控连线；
- 食材飞入锅；
- 掉落与补盘；
- 三次投料；
- 主动开火；
- 烹饪；
- 料理揭晓；
- 订单完成。

不得使用调试跳转、直接调用正确答案、加速、剪掉操作错误、浏览器脚本伪造触摸或静态图代替运行画面。

### 11.3 构建冒烟

使用 Cocos Creator 3.8.8 进行 Web Mobile 真实构建或预览冒烟。

D0 暂不要求 Android 构建；Android 属于 D3。

## 12. 报告

生成：

- `reports/cp0-d/d0/CP0D-D0-Test-Report.md`；
- `reports/cp0-d/d0/CP0D-D0-Test-Report.json`。

报告必须包含：

- 开工基线 SHA；
- 完成提交 SHA；
- Cocos 与 Node 版本；
- 配置哈希；
- 变更文件和新增素材；
- 所有测试命令及真实结果；
- 新增测试编号和结果；
- O1、O2_STANDARD、O3_STANDARD 最终快照；
- 存档迁移结果；
- 六张截图尺寸；
- 两段录屏尺寸、帧率和时长；
- C1 功能零回归证明；
- 未完成项和已知问题；
- 工作区状态。

禁止手写虚假的 PASS。报告数据必须来自实际命令或运行记录。

## 13. Git 要求

1. 不修改无关文件；
2. 不覆盖用户已有修改；
3. 将实现、素材、测试和报告纳入提交；
4. 提交信息建议：`feat: complete CP0-D D0 multi-scenario prototype`；
5. 提交后确认工作区干净；
6. 推送到当前 GitHub 远程分支；
7. 返回完整提交 SHA 和推送结果。

未经明确授权不得新建或合并 PR，不得改写历史。

## 14. 明确禁止

本次不得开发：

- O2_BLACK 可玩流程；
- O3_INSPIRATION 可玩流程；
- 黑暗料理揭晓；
- 灵感首次提示；
- 珍稀料理揭晓；
- Android 构建；
- 正式菜谱图鉴或食材图鉴；
- 正式首页；
- 长期养成；
- 金币、经验、体力；
- 道具、广告、内购；
- TapTap SDK；
- 人物、剧情、活动；
- 新食材、新料理、新订单、新锅具；
- CP0-D-D1、D2、D3；
- CP1 或 G4。

## 15. 停止条件

完成 D0 后立即停止，只返回：

1. D0 完成状态；
2. Git 基线与完成 SHA；
3. 推送结果；
4. 变更文件；
5. 测试结果；
6. 三个场景的确定性结果；
7. 存档迁移结果；
8. 六张截图；
9. 两段录屏；
10. 测试报告；
11. C1 零回归证明；
12. 未完成项或阻塞项；
13. 工作区是否干净。

不得开始 D1，等待人工验收与下一次明确授权。
