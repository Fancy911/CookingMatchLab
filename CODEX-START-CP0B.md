# Cooking Match Lab - Codex CP0-B Only Execution Instruction

Version: 1.0  
Stage: CP0-B  
Authorization status: Approved by the user  
Project: 《料理消消研究所》

## 0. 唯一授权

现在开始执行 **CP0-B：纯 TypeScript 核心规则、配置与确定性自动测试**。

本次只允许执行 CP0-B。不得开始 CP0-C，不得把规则接入当前 Cocos 对局界面，不得继续制作新 UI、美术、动画、音效、关卡或其他系统。

CP0-B 完成全部交付物和验收证据后必须停止，等待用户明确确认。用户未明确说“开始 CP0-C”之前，不得开发 CP0-C 或任何后续阶段。

规划文档中较早出现的“当前不向 Codex 下发开发任务”等历史状态，只被本次用户授权在 **CP0-B 范围内**覆盖；其中的玩法规则、内容数值和禁止扩展要求仍然有效。

## 1. 开工前检查

在写入任何文件前完成并记录：

1. 当前 Git 分支、HEAD commit hash 和工作区状态。
2. 确认 CP0-A 已经存在于仓库，且当前视觉验收版本可以正常打开。
3. 读取并记录项目实际锁定的 Cocos Creator 3.8.x 精确版本；不得升级或切换版本。
4. 确认以下完整文档真实存在并可读取：
   - `docs/00-project/Project-Baseline-v1.md`
   - `docs/01-visual/G1B-Visual-Direction-v1.md`
   - `docs/02-gameplay/Core-Gameplay-Rules-v1.1.md`
   - `docs/03-content/G2-MVP-Content-v0.2.md`
   - `docs/04-handoff/G3-Development-Handoff-v0.2.md`
5. 记录 CP0-A 场景、Prefab 和视觉资源目录的 Git 状态，作为完成后的无改动对照。

如果文档缺失、版本不一致、工作区存在无法判断归属的改动，或 CP0-A 基线无法确认，立即停止并报告，不得自行补写规则或覆盖现有改动。

## 2. 资料优先级

实现时按以下优先级读取：

1. `G3-Development-Handoff-v0.2.md`：CP0-B边界、架构、测试和交付要求。
2. `G2-MVP-Content-v0.2.md`：6种食材、6道料理、3个订单、5个固定场景、棋盘、补充队列、配方和数值。
3. `Core-Gameplay-Rules-v1.1.md`：通用玩法规则。
4. `G1B-Visual-Direction-v1.md`：本阶段只用于确认不得破坏CP0-A视觉，不产生新视觉工作。

发现文档之间存在无法按上述优先级消除的实质冲突时，停止并列出冲突，不得擅自拍板。

## 3. CP0-B目标

建立一套不依赖 Cocos 节点和画面的纯 TypeScript 规则内核，使以下结果可通过固定输入稳定复现：

- 连线路径是否合法；
- 合法连线如何消耗步数和占用一次投料位；
- 棋盘如何消除、掉落和按列补盘；
- 7连如何在固定位置生成灵感食材；
- 普通与灵感食材如何折算真实入锅单位；
- 何时允许主动开火；
- 同一锅输入如何确定生成唯一料理；
- 如何计算与订单目标无关的1～3星；
- 非目标料理揭晓后如何清锅并继续订单；
- 首次发现、最高星级和首次灵感提示状态如何保留；
- 固定测试场景如何输出可比较的状态快照与测试报告。

本阶段的核心成果是：**规则可验证、结果可复现、配置可修改、测试可重复运行**。

## 4. 允许实现的范围

### 4.1 纯 TypeScript 领域层

允许实现下列职责，命名可以遵循项目已有约定，但职责不得混在 Cocos Component 中：

- `BoardModel`
- `PathValidator`
- `BoardResolver`
- `InspirationResolver`
- `DeadBoardDetector`
- `ShuffleResolver`
- `PotModel`
- `RecipeResolver`
- `StarCalculator`
- `OrderResolver`
- `RunSnapshot`

为了完整执行固定场景，可以增加纯 TypeScript 的：

- `OrderSession` 或等价的无界面会话协调器；
- 路径编辑状态，用于处理追加节点和退回倒数第二格；
- 发现记录、最高星级和首次提示状态的纯数据模型或 reducer；
- 测试用内存状态仓库。

不得在 CP0-B 接入 Cocos `sys.localStorage`。真实设备存档适配器不属于本阶段。

### 4.2 配置与加载校验

允许建立并读取：

- `gameplay.json`
- `ingredients.json`
- `recipes.json`
- `orders.json`
- `tutorials.json`
- `scenarios/*.json`

配置必须带 `schemaVersion`，并由统一的 `ConfigRegistry` 或等价入口加载和校验。

### 4.3 确定性测试基础设施

允许实现：

- `ScenarioService`
- `PrototypeTestRunner` 或等价的无画面测试运行器
- `RunLogger`
- 可注入的 `DeterministicRng`
- 稳定快照序列化和 hash

固定测试场景必须使用文档给出的 `initialBoard` 和逐列 `columnQueues`，不得改成随机棋盘。

普通调试模式如确有需要，可使用 `xorshift32-v1` 和显式32位无符号 seed；玩法代码内禁止直接调用 `Math.random()`。

### 4.4 测试工具

- 使用 Node.js 20 或以上。
- 使用 Vitest。
- 优先沿用仓库已有包管理器和 lockfile；如果项目没有 lockfile，使用 npm 和 `package-lock.json`。
- 只增加完成本阶段所必需的开发依赖，不升级 Cocos Creator 或无关依赖。
- 领域层和被 Node 测试导入的应用层不得 import `cc`。

必须提供以下命令：

```text
npm test
npm run test:unit
npm run test:scenarios
npm run typecheck
```

## 5. 冻结配置

### 5.1 基础玩法

以下数值必须来自配置，不得写死在规则代码中：

```json
{
  "schemaVersion": 1,
  "board": {
    "rows": 7,
    "columns": 7,
    "ingredientTypeCount": 5,
    "connectionDirections": 8,
    "minimumLink": 3
  },
  "pot": {
    "baseSlots": 3,
    "temporarySlots": 1,
    "minimumThrowsToCook": 2
  },
  "longLink": {
    "fine": 5,
    "inspiration": 7,
    "master": 9
  },
  "inspiration": {
    "unitValue": 2,
    "spawnStrategy": "PATH_END_COLUMN_FIRST_NEW_CELL"
  }
}
```

第四临时投料位只保留配置能力。CP0-B 不实现玩家入口、广告入口或 Mock 按钮，任何基础配方和固定测试都不得依赖第四槽位。

### 5.2 食材

只允许以下6种食材：

- 番茄
- 鸡蛋
- 土豆
- 胡萝卜
- 蘑菇
- 香葱

食材 ID 必须沿用 G2 v0.2 已确认的稳定 ID，不得因显示名称重新发明另一套 ID。

### 5.3 料理

只允许以下6道料理：

- `RCP_TOMATO_EGG`：番茄炒蛋
- `RCP_SCALLION_POTATO_CAKE`：香葱土豆饼
- `RCP_GARDEN_MUSHROOM_SOUP`：田园菌菇汤
- `RCP_WARM_HOTPOT_MIX`：暖锅杂烩，唯一兜底料理
- `RCP_CHARRED_TOMATO_POTATO_BALL`：显示名为“黏糊番茄薯团”；稳定 ID 不改
- `RCP_STAR_MUSHROOM_EGG_CUP`：星辉菌菇蛋盅

所有食材范围、禁止食材、比例区间、理想值、处理条件和优先级必须逐项照录 G2 v0.2，禁止自行“合理化”或修改配方。

料理匹配规则：

1. 读取最终入锅单位和处理条件集合。
2. 检查所有非兜底料理。
3. 必需食材、禁止食材、数量范围、比例和硬条件必须全部通过。
4. 多个候选按优先级降序，再按稳定料理 ID 升序。
5. 没有明确配方通过时生成唯一兜底料理。
6. 料理身份不得读取当前订单目标、投料顺序或随机数。

### 5.4 订单

只允许：

- `ORD_01`：7步；食材池为番茄、鸡蛋、土豆、胡萝卜、蘑菇；目标番茄炒蛋。
- `ORD_02`：8步；食材池为土豆、鸡蛋、香葱、番茄、蘑菇；目标香葱土豆饼。
- `ORD_03`：8步；食材池为蘑菇、胡萝卜、土豆、鸡蛋、番茄；目标田园菌菇汤。

各订单的效率阈值逐项照录 G2 v0.2。

### 5.5 固定测试场景

只允许以下5个场景：

- `O1_TUTORIAL_001`
- `O2_STANDARD`
- `O2_BLACK`
- `O3_STANDARD`
- `O3_INSPIRATION`

每个场景的7×7初始棋盘、逐列补充队列、推荐路径和期望结果必须直接照录 G2 v0.2 的完整正文。

不得：

- 缩短棋盘；
- 用数量袋替代逐列队列；
- 用 PRNG seed 替代固定场景队列；
- 在队列耗尽时随机补盘；
- 为了让测试通过而修改推荐路径或期望结果。

固定队列中，队首元素落入该列本次新增空格中的最低位置。队列提前耗尽属于配置错误，必须明确失败。

## 6. 关键实现口径

### 6.1 连线

- 默认允许横、竖、斜八方向相邻。
- 四方向模式必须仍可通过配置切换，并用于单元测试。
- 最低3连。
- 少于3格时取消，不消耗步数，不修改棋盘、锅、队列和任何记录。
- 路径不能重复使用同一格。
- 手势退回路径倒数第二格时，只移除最后一格。
- 只有玩家主动提交的合法路径进入锅中。
- 补盘不得自动消除、自动投料或触发自动连锁。

### 6.2 消除、掉落与补盘

- 每列独立向下掉落。
- 同列未消除食材的相对顺序保持不变。
- 补盘严格从该列固定队列读取。
- 一个合法连线消耗1步，并占用1个投料位，不按连接格数量重复计步或占槽。

### 6.3 灵感食材

- 7连产生灵感食材，但灵感食材不进入触发它的当前投料。
- 先结算当前投料，再消除、掉落和补盘。
- 灵感食材优先生成在本次路径终点所在列的第一个新补格。
- 该列没有合资格新补格时，按文档固定顺序选择其他新补格。
- 灵感食材参与后续连线时只占1格，入锅时按2单位同类食材计算。
- 9连的大师处理标签不等同于灵感；需要灵感条件的料理不能仅凭大师标签命中。

### 6.4 锅与开火

- 每锅默认3次投料。
- 至少完成2次合法投料后才允许主动开火。
- 完成第3次投料后不得自动开火。
- 开始下一锅不额外消耗步数；只有新的合法连线消耗步数。
- MVP配方不读取投料顺序。

### 6.5 非目标料理

主动开火后：

- 命中订单目标：订单完成并结算。
- 生成非目标料理：仍计算料理、星级、发现记录和最高星级。
- 非目标料理且仍有剩余步数：清空锅中食材、处理标签和本锅投料次数；保留棋盘、棋盘特殊食材、补充队列进度、剩余步数和订单目标，继续当前订单。
- 非目标料理且剩余步数为0：进入订单未完成结果。
- 订单失败不得回滚首次发现、黑暗料理记录或最高星级。

### 6.6 星级

按 G2 v0.2 冻结公式实现：

- 数量差 `D = Σ |实际数量 - 理想数量|`。
- 数量分 `max(0, 100 - 12.5 × D)`。
- 比例位于理想区间为100分；仅位于可接受区间为80分。
- 明确配方匹配分为数量分70%＋比例分30%。
- 兜底料理的配方匹配分固定50，最终总分上限70，最高2星。
- 单次投料处理分：3～4连为60；5～6连为80；7～8连为90；9连及以上为100。
- 本锅采集过灵感食材时，处理分加10，最高100。
- 本锅处理分为各次投料处理分平均值。
- 效率分读取当前订单配置的剩余步数阈值：高档100、中档70、低档40。
- 总分为配方匹配60%＋处理质量25%＋步数效率15%。
- 总分不低于85为三星；不低于65为二星；否则一星。

同样一锅料理不能因为它是否为当前订单目标而得到不同星级。

### 6.7 死盘洗牌

- 死盘判定只读取当前棋盘和当前连接规则。
- 死盘自动免费洗牌，不消耗步数，不修改锅。
- 洗牌必须通过注入的确定性 RNG 完成，保留棋盘食材多重集合，并确保产出至少一条合法路径。
- 可以设置配置化最大尝试次数；超过次数必须明确失败，不得回退到 `Math.random()` 或暗中改造食材数量。
- 在报告中记录实际采用的确定性洗牌算法。

## 7. 配置校验

至少校验：

- 棋盘行列数准确。
- 固定场景棋盘只包含该订单食材池。
- 每个场景有7条非空列队列。
- 配方引用的食材、锅具和条件 ID 合法。
- 只有一个兜底料理。
- 非兜底配方不存在未被优先级明确解决的重叠。
- 三个目标料理都能在默认3次投料内完成。
- 固定场景推荐路径全部合法。
- 必填字段、枚举、范围和 ID 引用完整。

CP0-A只制作了番茄炒蛋相关视觉资源，其他5道料理和香葱等后续资源尚未生产。因此 CP0-B：

- 只对未来资源 ID/路径做结构校验；
- 不得因为未来美术文件尚不存在而伪造图片、色块或 Emoji；
- 不得把未来美术缺失误报为本阶段已完成；
- 在测试报告中明确列为“按阶段计划延后”，不作为纯规则测试失败；
- 真实资源存在性和加载验收留给对应素材与运行时阶段。

## 8. 必须通过的单元测试

测试编号和意图必须保留，报告中逐项列出：

- U01：八方向相邻合法；切换为四方向后斜线非法。
- U02：少于3连取消，所有状态不变。
- U03：路径不可重复；返回倒数第二格只撤销最后格。
- U04：普通食材1格等于1入锅单位。
- U05：灵感食材连接占1格、入锅等于2单位。
- U06：5、7、9长连阈值从配置读取。
- U07：7连先完成当前投料，再在补盘生成灵感。
- U08：灵感位于路径终点列的第一个新补格。
- U09：掉落保持同列相对顺序。
- U10：补盘不自动消除、不自动入锅。
- U11：死盘免费洗牌，不改变步数和锅。
- U12：一次合法连线只占一个投料位。
- U13：至少两次投料才能开火；第三次后不自动开火。
- U14：投料顺序不同但最终输入相同，生成同一道料理。
- U15：G2冲突表 C01～C12 全部通过。
- U16：0～12整数枚举范围内，不存在两个明确配方同时命中。
- U17：五个明确配方的理想输入均为三星。
- U18：兜底总分最高70，因此最多二星。
- U19：星级计算不读取当前订单目标身份。
- U20：非目标料理且有剩余步数时只清锅，保留棋盘、特殊食材、步数和队列进度。
- U21：非目标料理且剩余0步时订单未完成。
- U22：订单失败不回滚首次发现和历史最高星级。
- U23：缺字段、非法食材或固定队列耗尽时明确失败。
- U24：同一配置、seed和操作序列生成相同快照 hash。

G2冲突表必须逐项实现：

- C01：番茄5、鸡蛋4 → 番茄炒蛋。
- C02：番茄6、鸡蛋5，即使处于其他订单 → 番茄炒蛋。
- C03：番茄7、鸡蛋4；只生成但未采集灵感 → 暖锅杂烩。
- C04：土豆6、鸡蛋3、香葱3 → 香葱土豆饼。
- C05：蘑菇5、胡萝卜4、土豆3 → 田园菌菇汤。
- C06：番茄7、土豆7、蘑菇4 → 黏糊番茄薯团。
- C07：蘑菇11、鸡蛋4且拥有灵感条件 → 星辉菌菇蛋盅。
- C08：蘑菇11、鸡蛋4但无灵感条件 → 暖锅杂烩。
- C09：同上，只有大师条件但无灵感条件 → 暖锅杂烩。
- C10：番茄5、鸡蛋4、蘑菇3 → 暖锅杂烩。
- C11：黏糊番茄薯团输入同时拥有灵感条件 → 黏糊番茄薯团。
- C12：任意合法但不匹配明确配方的输入 → 暖锅杂烩。

## 9. 必须通过的固定场景集成测试

- S01：O1按番茄5连、鸡蛋4连，生成番茄炒蛋，剩余5步。
- S02：O2_STANDARD按土豆6、鸡蛋3、香葱3，生成香葱土豆饼，剩余5步。
- S03：O2_BLACK第一锅按番茄7、土豆7、蘑菇4，生成黏糊番茄薯团，剩余5步。
- S04：O2_BLACK清锅后继续，按土豆6、鸡蛋3、香葱3生成订单目标，剩余2步。
- S05：O3_STANDARD按蘑菇5、胡萝卜4、土豆3，生成田园菌菇汤，剩余5步。
- S06：O3_INSPIRATION完成7连后，灵感蘑菇固定生成在文档指定的 `r2c4`；后续连接3格时实际加入4单位蘑菇。
- S07：O3_INSPIRATION第一锅得到蘑菇11、鸡蛋4和灵感条件，生成星辉菌菇蛋盅，剩余5步。
- S08：第一锅清锅后继续，按蘑菇5、胡萝卜4、土豆3生成订单目标，剩余2步。
- S09：重复执行时首次灵感提示不重复出现，料理判定仍相同。

## 10. 快照和报告

每次场景操作至少记录：

- Cocos Creator精确版本；
- Git基线 commit；
- `schemaVersion` 和稳定 `configHash`；
- `scenarioId`；
- RNG算法和seed，固定队列场景注明未使用PRNG补盘；
- 操作前后剩余步数；
- 操作前后锅中单位；
- 已使用投料位；
- 处理标签；
- 队列读取位置；
- 稳定 `boardHash`；
- 最终料理、星级和订单结果；
- PASS/FAIL；
- 失败时第一处差异。

坐标内部和JSON使用0基；面向人的报告使用 `r1c1` 格式。

必须生成机器可读 JSON 和人类可读 Markdown：

- `reports/cp0-b/CP0B-Test-Report.json`
- `reports/cp0-b/CP0B-Test-Report.md`

不得只在控制台打印结果。

## 11. 交付物

CP0-B完成时必须交付：

1. 纯 TypeScript 领域层、必要的无界面应用协调层和测试基础设施。
2. 完整配置：
   - 6种食材；
   - 6道料理；
   - 3个订单；
   - 5个固定测试场景；
   - G2 v0.2全部固定棋盘和逐列补充队列。
3. U01～U24自动测试。
4. S01～S09固定场景集成测试。
5. 可重复执行的npm脚本和唯一lockfile。
6. JSON与Markdown测试报告。
7. 一张真实390×844运行冒烟截图，证明CP0-A原对局画面仍可打开：
   - `reports/cp0-b/CP0B-01-Battle-Smoke-390x844.png`
   - 该截图不要求新视觉内容，画面应保持CP0-A验收状态。
8. 最终变更文件清单和Git diff范围说明。

如果工程已采用其他合理目录约定，可以沿用，但领域层、配置层、测试层和Cocos表现层必须清晰隔离。

## 12. 验收标准

只有同时满足以下条件才可声明 CP0-B 完成：

- `npm test` 通过。
- `npm run test:unit` 通过。
- `npm run test:scenarios` 通过。
- `npm run typecheck` 通过。
- U01～U24共24项全部通过。
- S01～S09共9项全部通过。
- 同一固定场景重复运行得到相同料理、星级、棋盘快照和hash。
- 规则层与Node测试没有 `cc` import。
- 玩法代码没有直接 `Math.random()`。
- 没有改动CP0-A场景、Prefab、UI布局和视觉资源。
- 没有新增或伪造香葱、其余5道料理、灵感演出、黑暗演出、音效或其他未来素材。
- 没有增加第7种食材、第7道料理、第4个订单或新玩法。
- 没有把规则接入Battle场景。
- 测试报告和390×844冒烟截图真实存在。

如果任何测试尚未通过，只能报告“CP0-B未完成”，不得用“基本完成”“主流程可用”替代验收。

## 13. 明确禁止

CP0-B不得执行：

- 修改当前390×844 UI布局；
- 修改CP0-A三张验收状态；
- 编写Cocos对局控制器、触摸输入、连线绘制或运行时动画；
- 把领域逻辑绑定到节点、Prefab或场景；
- 制作食材飞锅、掉落、补盘、开火、揭晓等视觉时序；
- 使用 imagegen 或新增任何美术素材；
- 新增音效、背景音乐或粒子；
- 接入真实广告、Mock广告、支付、TapTap SDK或统计SDK；
- 开发图鉴、商店、养成、厨房装修、活动、剧情、人物或更多锅具；
- 扩充食材、料理、订单或固定场景；
- 修改已冻结配方和数值以迁就实现；
- 开始 CP0-C 或撰写 CP0-C 代码。

## 14. 完成后的唯一回复格式

完成后向用户报告：

1. CP0-B是否完整通过。
2. 基线与完成后的Git commit hash。
3. Cocos Creator和Node精确版本。
4. 变更文件清单。
5. 四条npm命令的实际结果。
6. U01～U24逐项结果。
7. S01～S09逐项结果。
8. 两份测试报告路径。
9. 390×844冒烟截图路径。
10. CP0-A场景和视觉资源无改动的Git diff证据。
11. 未完成项或阻塞项；没有则写“无”。

随后停止。不得主动提出或开始 CP0-C。
